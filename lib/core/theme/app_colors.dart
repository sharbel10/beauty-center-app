import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  static const Color primary = Color(0xFF1F3A5F);
  static const Color secondary = Color(0xFFC6A769);
  static const Color tertiary = Color(0xFF162B47);
  static const Color neutral = Color(0xFFF7F9FB);
  static const Color error = Color(0xFFD84040);
  static const Color success = Color(0xFF2E7D52);
  static const Color textDark = Color(0xFF1A1A2E);
  static const Color textLight = Color(0xFF8A94A6);
  static const Color white = Color(0xFFFFFFFF);
  static const Color scaffold = Color(0xFFF7F9FB);

  // Home UI aliases (design kit tokens mapped to app palette).
  static const Color gold = secondary;
  static const Color surface = white;
  static const Color surfaceMuted = Color(0xFFE8EDF3);
  static const Color divider = Color(0xFFE2E8F0);
  static const Color primarySoft = primary;
  static const Color danger = error;
  static const Color textMuted = textLight;
  static const Color textSecondary = Color(0xFF6B7280);
}
